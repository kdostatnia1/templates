::MC basic::
What is the difference between cloning and forking a repository?  {
	~they are all the same
    =forks have the same history but divergent futures
    ~forking is not a Git term
}


Git is a management system. {T} 